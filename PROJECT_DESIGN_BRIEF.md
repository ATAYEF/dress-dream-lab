# استایلر (Style Matcher / Dress Dream Lab) — راهنما، قواعد و بریف طراحی

> سند واحد برای بازطراحی یا ادامه توسعه توسط انسان یا هوش مصنوعی دیگر.  
> ریپو: `https://github.com/ATAYEF/dress-dream-lab`  
> پیش‌نمایش: `https://dress-dream-lab.lovable.app`  
> زبان رابط: **فارسی (RTL)**

---

## ۱. خلاصه محصول

**استایلر** یک وب‌اپ کمد لباس هوشمند است که:

1. عکس کاربر (پروفایل) و عکس لباس‌ها را می‌گیرد.
2. لباس‌ها را روی **مانکن / پرو مجازی** می‌چیند.
3. با توجه به **مناسبت، محیط و آب‌وهوا** از کمد کاربر **ست پیشنهاد** می‌دهد.
4. متن پیشنهاد را با هوش مصنوعی (یا الگوریتم محلی) تولید می‌کند.
5. هماهنگی **رنگ، کفش و اکسسوری** را پیشنهاد می‌دهد.

**هسته ارزش:** نه یک گالری لباس ساده، بلکه **ست‌ساز + پرو مجازی + پیشنهاد هوشمند از کمد واقعی کاربر**.

---

## ۲. استک فنی (الزام حفظ مگر تصمیم صریح به تعویض)

| لایه | فناوری |
|------|--------|
| فرانت | React 18 + TypeScript + Vite |
| استایل | Tailwind CSS + CSS variables + shadcn/ui (Radix) |
| روتینگ | react-router-dom |
| بک‌اند / دیتا | Supabase (Auth, Postgres, Storage, Edge Functions) |
| مهمان | localStorage (بدون لاگین) |
| فونت | Vazirmatn (RTL) |
| تم | Light / Dark (`next-themes`) |

**اسکریپت‌ها:** `npm run dev` | `npm run build` | `npm run preview`

---

## ۳. مدل داده اصلی

```ts
type ClothingCategory = 'tops' | 'bottoms' | 'shoes' | 'accessories' | 'outerwear' | 'dresses';

interface ClothingItem {
  id: string;
  name: string;
  category: ClothingCategory;
  imageUrl: string;      // مسیر پایدار storage یا URL/asset باندل‌شده
  color?: string;        // ترجیحاً نام فارسی رنگ
  tags?: string[];
  createdAt: Date;
}

type OutfitStyle = 'formal' | 'party' | 'casual';
type OutfitEnvironment = 'office' | 'gathering';
type OutfitWeather = 'sunny' | 'rainy' | 'cold';

interface OutfitSuggestion {
  id: string;
  items: ClothingItem[];
  suggestionText?: string;
  generatedImageUrl?: string;
  isFavorite?: boolean;
  createdAt: Date;
  context?: { style: OutfitStyle; environment: OutfitEnvironment; weather: OutfitWeather };
}

interface UserProfile {
  imageUrl: string | null;
  name?: string;
}
```

### Supabase (کاربر لاگین‌شده)

- جداول تقریباً: `clothing_items`, `outfit_suggestions`, پروفایل کاربر
- Storage: باکت خصوصی تصاویر؛ ترجیح ذخیره **path** نه signed URL منقضی
- Edge Function: `generate-outfit` (پیشنهاد متنی با context مناسبت)

### مهمان

- کلیدهای localStorage مثل `styler_clothes`, `styler_suggestions`, `styler_profile`
- کمد نمونه (`SAMPLE_CLOTHES`) با تصاویر **باندل‌شده داخل اپ** (بدون API تصویر خارجی در runtime)

---

## ۴. معماری فرانت (وضعیت هدف فعلی)

### صفحات

- `/` — `Index.tsx` (اپ اصلی)
- `/auth` — ورود / ثبت‌نام
- `*` — 404

### هوک مرکزی

- `useWardrobe` — لباس، پیشنهاد، پروفایل، CRUD، generate، مهاجرت مهمان→کاربر، logout reset

### اجزای کلیدی

| کامپوننت | نقش |
|----------|-----|
| `OutfitBuilder` | **هسته UI**: مانکن + گالری + شرایط ست + ساخت پیشنهاد |
| `MannequinDisplay` | نمایش لباس روی مانکن / عکس پروفایل |
| `OutfitContextPicker` | نوع لباس / محیط / آب‌وهوا |
| `CategoryTabs` | منوی کشویی دسته (**پیش‌فرض بسته**) |
| `ColorHarmonyBadge` | امتیاز و پیشنهاد هماهنگی رنگ |
| `ShoeSuggestions` / `AccessorySuggestions` | کفش و اکسسوری هماهنگ |
| `ProfileSection` | خوش‌آمد + آپلود عکس کاربر |
| `OutfitSuggestionCard` | کارت ست پیشنهادی |
| `AddClothingModal` / `ClothingDetailsModal` | افزودن / ویرایش / جزئیات |
| `MobileFab` | افزودن سریع روی موبایل |

### کتابخانه‌های دامنه (`src/lib`)

- `outfitContext.ts` — امتیازدهی و ساخت ست بر اساس شرایط
- `colorHarmony.ts` — هارمونی رنگ
- `shoeSuggestion.ts` / `accessorySuggestion.ts` — پیشنهاد کفش/اکسسوری
- `storage.ts` — resolve مسیر تصویر
- `sampleWardrobe.ts` — کمد دمو
- `categoryConfig.ts` — لیبل/رنگ/آیکن دسته‌ها

---

## ۵. جریان صفحه اصلی (ترتیب اجباری محتوا)

1. **هدر** چسبان: لوگو «استایلر»، تم، میان‌بر ست‌ساز، افزودن لباس، ورود/خروج  
2. **خوش‌آمد / پروفایل** (`ProfileSection`) — بالای صفحه، جمع‌وجور  
3. **ست‌ساز هوشمند** (`#outfit-builder`) — بخش اصلی و برجسته  
4. **ست‌های پیشنهادی** (در صورت وجود)  
5. CTA ورود برای مهمان (در صورت نیاز)  
6. FAB موبایل + اسکرول به بالا + بنر «در حال ساخت ست»

### ❌ حذف‌شده / ممنوع در بازطراحی مگر درخواست صریح

- بخش جدا به نام **«کمد لباس دیجیتال»** با گرید مدیریت مستقل  
- دوبله کردن منوی دسته در چند جای صفحه  
- تصاویر نمونه وابسته به CDN/API ناپایدار (مثل URLهای موقت AI)

### ✅ مدیریت لباس کجاست؟

**فقط داخل گالری ست‌ساز:**

- جستجو (نام، رنگ، تگ)
- فیلتر دسته با **منوی کشویی پیش‌فرض بسته**
- مشاهده / ویرایش / حذف روی هر آیتم
- انتخاب برای مانکن (کلیک / درگ)

یک منبع داده (`clothes`)، یک نمای مدیریت+انتخاب.

---

## ۶. قواعد UX / UI

### زبان و جهت

- تمام UI به **فارسی** و **`dir="rtl"`**
- اعداد قابل‌نمایش با `toLocaleString('fa-IR')` در جاهای کاربرپسند

### هویت بصری

- تم لوکس ملایم: **طلایی (gold)**، کرم، رز، اسپرسو
- گوشه‌های گرد زیاد، سایه نرم، گرادیان‌های طلایی
- فونت display برای تیترها
- Dark mode پشتیبانی شود

### موبایل (الزام)

- هدف لمسی ≥ ۴۴px
- ورودی جستجو ≈ ۱۶px (جلوگیری از زوم iOS)
- Safe area / `viewport-fit=cover`
- FAB و CTA اصلی ست روی موبایل در دسترس (sticky در صورت نیاز)
- منوی دسته بسته پیش‌فرض تا شلوغی کم شود
- مودال جزئیات نزدیک تمام‌صفحه روی موبایل

### خلوت بودن

- از تکرار بلوک‌های آماری/دسته‌ای یکسان در پروفایل و کمد خودداری
- اولویت بصری با **ست‌ساز و مانکن**
- فیلترها جمع‌وجور (کشویی، نه ردیف بلند تب‌ها)

---

## ۷. قواعد کسب‌وکار ست‌سازی

1. حداقل حدوداً **۲ لباس** برای تولید پیشنهاد (یا کمد کافی برای auto-build).  
2. شرایط:
   - نوع: روزمره / رسمی / مهمانی  
   - محیط: اداری / دورهمی  
   - آب‌وهوا: آفتابی / بارانی / سرد  
3. الگوریتم محلی می‌تواند ست را از کمد بچیند؛ AI متن را غنی می‌کند (با JWT برای edge).  
4. اگر کاربر آیتمی انتخاب کرده، به‌عنوان **anchor** در نظر گرفته شود.  
5. سرما/باران → ترجیح outerwear و کفش مناسب.  
6. مهمانی → لباس/پاشنه/کیف مناسب‌تر؛ اداری → رسمی‌تر.

---

## ۸. ذخیره‌سازی تصویر (قواعد فنی)

- برای کاربر لاگین: آپلود به Storage و ذخیره **path پایدار** در DB  
- در نمایش: `resolveImageUrl` برای signed URL تازه  
- مهمان: data URL یا asset محلی قابل قبول است  
- نمونه‌های دمو: فقط فایل داخل `src/assets` (PNG/JPG)

---

## ۹. اهداف بازطراحی (برای AI طراح)

اگر مأموریت **بازطراحی UI/UX** است، این‌ها را رعایت کن:

### باید حفظ شود

- RTL و فارسی بودن کامل
- هسته ست‌ساز + مانکن + پیشنهاد از کمد کاربر
- فیلتر مناسبت / محیط / آب‌وهوا
- کار بدون لاگین (مهمان) + ارتقا با لاگین
- دسته‌های شش‌گانه لباس
- تم طلایی/لوکس (می‌تواند مدرن‌تر شود ولی هویت طلایی حفظ شود)

### می‌تواند بهتر شود

- سلسله‌مراتب بصری خلوت‌تر و مدرن‌تر
- جریان onboarding کوتاه‌تر
- گالری داخل ست‌ساز خواناتر (جستجو + dropdown دسته)
- کارت‌های پیشنهاد واضح‌تر
- عملکرد موبایل (کمتر اسکرول، کمتر شلوغی)
- دسترسی‌پذیری (focus، کنتراست، aria)

### نباید

- اپ را به فروشگاه عمومی تبدیل کردن (این **کمد شخصی** است)
- حذف پرو مجازی / مانکن
- افزودن منوهای تکراری دسته در چند بخش
- برگرداندن «کمد دیجیتال» جدا مگر اینکه مدیریت داخل گالری حفظ شود و UI یکپارچه بماند

---

## ۱۰. نقشه فایل‌های مهم

```
src/
  pages/Index.tsx          # صفحه اصلی
  pages/Auth.tsx
  components/OutfitBuilder.tsx
  components/MannequinDisplay.tsx
  components/CategoryTabs.tsx
  hooks/useWardrobe.ts
  lib/outfitContext.ts
  lib/colorHarmony.ts
  lib/storage.ts
  types/wardrobe.ts
  index.css                # توکن‌های رنگ و گرادیان
supabase/functions/generate-outfit/
```

---

## ۱۱. چک‌لیست پذیرش بازطراحی

- [ ] RTL و فارسی درست است  
- [ ] ست‌ساز بالاترین اولویت بصری را دارد  
- [ ] گالری: جستجو + منوی دسته بسته پیش‌فرض + ویرایش/حذف  
- [ ] بخش کمد جداگانه شلوغ وجود ندارد  
- [ ] مناسبت/محیط/آب‌وهوا قابل انتخاب است  
- [ ] موبایل: FAB، لمس، بدون همپوشانی محتوا  
- [ ] مهمان با نمونه کار می‌کند؛ لاگین داده را پایدار می‌کند  
- [ ] تصاویر نمونه بدون وابستگی شبکه در runtime  

---

## ۱۲. پیام برای مدل بازطراحی (کپی‌پیست)

```
این پروژه «استایلر» است: کمد لباس شخصی RTL فارسی با React+Vite+Tailwind+Supabase.
هسته: ست‌ساز هوشمند + مانکن/پرو مجازی + پیشنهاد ست از لباس‌های خود کاربر
بر اساس مناسبت (روزمره/رسمی/مهمانی)، محیط (اداری/دورهمی)، آب‌وهوا (آفتابی/بارانی/سرد).
مدیریت لباس فقط داخل گالری ست‌ساز است (جستجو، منوی کشویی دسته پیش‌فرض بسته، ویرایش/حذف).
بخش جدا «کمد دیجیتال» حذف شده و نباید برگردد مگر یکپارچه با همان گالری.
تم لوکس طلایی/کرم، موبایل‌فرست، بدون شلوغی و بدون منوی تکراری.
هوک useWardrobe منبع حقیقت داده است. مهمان=localStorage، کاربر=Supabase.
بازطراحی UI/UX را با حفظ این قواعد انجام بده؛ استک را عوض نکن مگر لازم باشد.
```

---

*نسخه سند: هم‌راستا با کامیت‌های شاخه main شامل ادغام گالری و بهینه‌سازی موبایل.*
